// Package proxy implements the transparent proxy mode.
// In this mode, snitchproxy sits between the app and real external APIs,
// forwarding all traffic while inspecting it against assertions.
package proxy
